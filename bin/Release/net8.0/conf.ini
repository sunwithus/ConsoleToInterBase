// настройки конфигурации (поместить должен находиться в папке сборки проекта)

// ffmpeg.exe
ffmpegExePath=C:\dotnet\ffmpeg\ffmpeg.exe

// путь к временному хранению файлов для конвертирования
path=C:\temp\2

// параметры подключения к БД Interbase
User=SYSDBA;
Password=masterkey;
Database=c:\dotnet\db38.ibs;
DataSource=localhost;

// путь к аудио/видео файлам (формат любой)
sourceFolderPath=C:\dotnet\wav

// перенести файлы после выполнения скрипта в другую папку (оставить пустой, если не переносить)
destinationFolderPath=

// удалять файлы после выполненпия скрипта (если значение destinationFolderPath не установлено)
deleteAfterComplete=0

// делать попытку спарсить из названия идентификаторы
tryToParse=1